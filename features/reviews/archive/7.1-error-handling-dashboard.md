# Review — 7.1 Error Handling & Dashboard Redesign

## Critical

- [x] **C-1.** B-1 not resolved for admins — `GET /health` returns 503 when storage unreachable → `healthQuery.isError` → generic error display hides per-component breakdown. Fix: change `/health` to return 200 with `status: "degraded"` (match `/health/status` contract). **Fixed:** `/health` now always returns 200; body carries status.
- [x] **C-2.** Spec migration table is false — `use-login.ts`, `use-settings.ts`, `use-two-factor.ts` (and `use-reset-password.ts`, `use-forgot-password.ts`, `use-secure-configs.ts`) still use `error.response?.data?.error` directly. Complete the migration. **Fixed:** Verified only `use-login.ts` was affected (reviewer overcounted); migrated to `parseApiError` + `ErrorCodes`.

## Important

- [x] **I-1.** Dead ad-hoc error code literals: `"COPY_FAILED"` in `reverse-share/upload.service.ts:383` and `"DISK_SPACE_ERROR"` in `storage/service.ts:161` — not in registry. **Fixed:** Added to registry, replaced literals, fixed raw message leak.
- [x] **I-2.** `diskSpaceQuery` and `adminStatsQuery` have no `refetchInterval` — go stale silently while health badge keeps polling. **Fixed:** Added `refetchInterval: 60_000`.
- [x] **I-3.** `AdminView` loading skeleton uses `&&` (all three loading) — should be `||` (any loading). **Fixed.**
- [x] **I-4.** `formatErrorForDisplay` leaks internal detail keys in `description` — drop description or use allow-list. **Fixed:** Removed `description` field entirely.
- [x] **I-5.** `useSystemStatus.refresh()` ignores returned promises — no loading feedback on refresh button. **Fixed:** Added `isRefreshing` state, disabled button + spin animation during refetch.
- [x] **I-6.** `useEffect` in `use-public-share.ts` has `t` in deps — may re-fire toast every render after error. **Fixed:** Removed `t` from deps with biome-ignore comment.
- [x] **I-7.** `getAdminStats`/`getHealthStatus` return-type mismatch (pre-existing pattern propagated) — note for future. **Skipped:** Pre-existing pattern, out of scope.

## Minor

- [x] **M-1.** Unused `ClientErrorCodes` import overlap in `use-uppy-upload.ts` — redundant with `isNetworkError`. **Fixed:** Simplified to `isNetworkError`, removed unused import.
- [x] **M-2.** `parseApiError` swallows empty-string `code` silently — add warning log. **Fixed:** Added `logger.warn()`.
- [x] **M-3.** `health-status.integration.test.ts` missing "storage configured but unreachable" test case. **Fixed:** Added test with mocked S3 client.
- [x] **M-4.** Dashboard `UserView` personal metrics hardcode `"—"` instead of using existing files/shares data. **Fixed:** Wired `fileCount` and `activeShareCount` from dashboard data.
- [x] **M-5.** `AdminError` only renders when `healthError` — `adminStatsError`/`diskSpaceError` fail silently. **Fixed:** Added `adminStatsError` with inline error display.
- [x] **M-6.** `use-system-status.ts` builds magic string error codes locally — use typed union or boolean. **Fixed:** Created `DashboardError` union type.
- [x] **M-7.** `system-status.tsx` storage state map duplicates structure 4 times — DRY into helper. **Fixed:** Extracted `getStorageStatusDisplay()` helper.
- [x] **M-8.** Health endpoint test uses regex `OR` instead of exact assertion — use `toBe("degraded")`. **Fixed.**
