# Review: 6.2 UI Code Quality Fixes

## Critical

- [x] [RC-1] `status-icon.tsx:22,24,26` — Uses hardcoded `text-blue-500`, `text-green-500`, `text-red-500` without dark mode variants. Should use `text-primary` / `text-emerald-600 dark:text-emerald-400` / `text-destructive`.

- [x] [RC-2] `create-reverse-share/field-requirements-section.tsx:150-159` AND `edit-reverse-share/field-requirements-section.tsx:147-156` — Hardcoded English strings ("Field Configuration:", "• Hidden: Field won't appear...") while `t()` is in scope. Add i18n keys.

- [x] [RC-3] Dark mode variants missing on ~20 status color instances across: `share-details-security-section.tsx` (yellow badge missing dark), `shares-table.tsx` (emerald hover missing dark), `edit-password-modal.tsx:132` (yellow-600 no dark), several delete buttons (`text-red-600 hover:text-red-700 hover:bg-red-50` — should use `text-destructive`). Note: `file-icons.tsx` (53 entries) and properly-paired status colors are INTENTIONAL — only fix cases missing dark variants.

## Important

- [x] [RI-1] `share-details/` subfolder has 4 files but `share-details-info-section.tsx` and `share-details-files-list.tsx` remain in `modals/` directly. Move them into the subfolder.

- [x] [RI-2] Icon sizing inconsistency in extracted files: `create-reverse-share/*.tsx` uses `const ICON_SIZE = 16` + `size={ICON_SIZE}` prop, while convention (Task 3.16) is `className="size-4"`. Convert to Tailwind.

- [x] [RI-3] `register-with-invite/[token]/page.tsx:64,95`, `(home)/page.tsx:16`, `login/page.tsx:25` still use `h-screen`. Auth pages with form content overflow on small viewports → `min-h-screen`.

- [x] [RI-4] `status-icon.tsx` API: `className` prop is consumed two ways (size for icons, merged for spinner). Rename to `sizeClass` or document constraint.

- [x] [RI-5] 7 new `loading.tsx` files + `Spinner` + `Loader` all default to English "Loading..." sr-only text. Pass `t("common.loading")` from callers, or make label required.

- [x] [RI-6] `use-public-share-navigation.ts:170-176` — `filteredFolders`/`filteredFiles` recomputed every render without `useMemo`. Add memoization.

- [x] [RI-7] `use-public-share-navigation.ts:179-187` — URL sync useEffect depends on `share` object reference, causing reset on TanStack Query refetch. Key on `share?.id` instead.

- [x] [RI-8] `editable-field.tsx:69` — `text-emerald-600 hover:text-emerald-700` missing dark variant. Add `dark:text-emerald-400 dark:hover:text-emerald-300`.

## Minor

- [x] [RM-1] 7 identical `loading.tsx` files — consider shared `export { default } from "@/components/layout/page-loading"` pattern.
- [x] [RM-2] `icon-picker/icon-cell.tsx:36` — `Icon` in useEffect deps is redundant (guarded by `if (Icon !== undefined) return`).
- [x] [RM-3] `icon-picker/icon-loader.ts:11` — Cache key is `iconName` only, should be `${slug}:${iconName}` for correctness.
- [x] [RM-4] `icon-picker/index.tsx:94-100` — `(pack.icons as unknown as string[])` → use `[...pack.icons]` instead.
- [x] [RM-5] `share-details-qr-section.tsx:48,67` — `defaultValue` parameter to `t()` is dead code (keys exist).
- [x] [RM-6] `register-form.tsx:71-78` — Error routing matches on English server error substrings. Pre-existing; note for follow-up.
- [x] [RM-7] `lib/app-info.ts:23` — Hardcoded `localhost:3333` fallback. Add comment or throw in production.
- [x] [RM-8] `utils/qr-download.ts:48` — `btoa()` only handles Latin-1. Use Blob+URL.createObjectURL for robustness.
- [x] [RM-9] `hooks/use-folder-crud.ts:57` — Unvalidated user input in S3 object key. Pre-existing; note for follow-up.
- [x] [RM-10] `share-details-modal.tsx:99` — `invalidateShare` missing from useEffect deps. Pre-existing.
- [x] [RM-11] Badge patterns (yellow/green/blue) duplicated in 4+ locations. Extract `StatusBadge` component.
- [x] [RM-12] `icon-picker/icon-grid.tsx:41-44` — `setTimeout(..., 50)` magic number, needs comment or constant.
