# 9.1 — Quickshare

**Status:** Spec
**Depends on:** —

## Summary

A zero-friction file sharing block on the dashboard that lets authenticated users
share files in one gesture: drop files, optionally tweak settings, click "Partager".

## Design Decisions

- **Placement:** Primary block on the dashboard, above existing blocks (RecentFiles, RecentShares, QuickAccessCards)
- **UX model:** Inline state transitions (no modal) — dropzone → upload+options → confirmation
- **Complementary:** The existing "Create Share" flow (modal, select existing files) remains for advanced use cases
- **GlobalDropZone removal:** The current `GlobalDropZone` wrapping the entire dashboard is removed. Drop zones are scoped to explicit areas only (Quickshare block, UploadFileModal).

## Three States

### State 1 — Dropzone (initial)

A prominent drag-and-drop zone — the visual centerpiece of the dashboard.
- Dashed border, icon (CloudUpload or similar), text: "Déposez vos fichiers ici pour partager rapidement"
- Click to open file picker as alternative to drag-and-drop
- Follows existing UI design language (card style, indigo accent)
- Drag-enter/leave uses a counter pattern (increment on `dragenter`, decrement on `dragleave`) to prevent child-element flicker — standard HTML5 DnD gotcha

### State 2 — Upload + Options (after drop)

The block transforms into two sections:

**Top section — File list:**
- Each file: name, size, circular progress indicator, remove button
- Ability to drop/add more files while uploading
- Upload starts immediately on drop (reuses `useFileUpload` / `useUppyUpload`)

**Bottom section — Options (all optional, pre-filled with defaults):**
- **Name** — pre-filled: filename if single file, "X fichiers" if multiple. Editable.
- **Expiration** — dropdown: 1 day, 7 days (default), 30 days, never
- **Password** — toggle switch, password field appears when enabled. Off by default.
- **Recipients** — email input, simplified version of `RecipientSelector`. Collapsible. Empty by default.

**"Partager" button** — always visible and clickable:
- If upload is complete: creates share immediately
- If upload still in progress: button label changes to "Partage en cours..." with spinner, share is created automatically when all uploads finish
- If any file has errors when all uploads finish (mixed success/error): auto-share is aborted, toast error displayed, user stays in State 2 to retry or remove failed files
- If all files are removed (via X buttons): automatically resets to State 1

**Recipients section** — only visible when SMTP is configured (`smtpEnabled !== "false"`). Uses the same email validation as `RecipientSelector` (`/^[^\s@]+@[^\s@]+\.[^\s@]+$/`). Invalid emails are rejected inline with a toast.

### State 3 — Confirmation (after successful share)

The block transforms into a success screen:
- Animated check icon
- Share link displayed prominently with "Copier le lien" button
- QR code of the link
- Summary line: "3 fichiers · expire dans 7 jours · pas de mot de passe"
- "Nouveau partage" button to reset to State 1

## Defaults (zero-config sharing)

| Setting | Default | Rationale |
|---------|---------|-----------|
| Name | Filename or "X fichiers" | Descriptive without effort |
| Alias | Auto-generated random slug | Every quickshare gets a shareable link |
| Expiration | 7 days | Safe default, avoids forgotten shares |
| Password | None | Minimal friction |
| Recipients | None | Optional, not pushed |

## Technical Flow

1. User drops file(s) on Quickshare block (dedicated dropzone, `GlobalDropZone` is removed)
2. Files added to Uppy, upload starts immediately via `useFileUpload` (presigned URL → S3 PUT → `registerFile`)
3. `registerFile` response includes the DB file ID; the upload hook captures it in `FileUploadState.registeredFileId`
4. User can optionally adjust name, expiration, password, recipients
5. User clicks "Partager":
   - If all uploads complete with no errors: call `createShare` with file IDs + options, then `createShareAlias` for the link
   - If uploads pending: register intent (`pendingShare` flag), auto-create share when all complete
   - If mixed success/error when pending share resolves: abort, show toast, stay in State 2
6. Share created → alias generated → `onShareCreated` callback fires → transition to State 3
7. If recipients provided and SMTP is enabled: auto-trigger `notifyRecipients` after share creation (non-blocking)

### Edge Cases

- **Quota exceeded / file too large**: handled by `useFileUpload.onValidate` → file enters `error` status with toast. User can remove it and proceed with successful files.
- **All files removed**: auto-reset to State 1 (dropzone)
- **SMTP disabled**: recipients section hidden entirely
- **Double-fire prevention**: `pendingShare` flag is cleared synchronously before calling `performShare`, preventing duplicate share creation
- **Multi-batch additions**: upload auto-start uses a reactive `useEffect` on `fileUploads` (not per-call `setTimeout`), safe for consecutive drops
- **Auto-name tracking**: a `nameTouched` ref tracks whether the user manually edited the name. Auto-rename on batch additions only fires when the user hasn't edited the name.

## Scope — Changes Required

### New
- `QuickShare` component (3-state block with dropzone, upload progress, options, confirmation)
- `useQuickShare` hook (orchestrates upload → share creation → alias generation)

### Modified
- `dashboard/page.tsx` — add `QuickShare` as primary block, remove `GlobalDropZone` wrapper
- `dashboard/components/` — reorder existing blocks below QuickShare
- `useUppyUpload` — add `registeredFileId` to `FileUploadState`, capture ID from `onAfterUpload` return
- `useFileUpload` — return DB file ID from `registerFile()` response in `onAfterUpload`

### Removed
- `GlobalDropZone` wrapper from dashboard — component itself (`components/general/global-drop-zone.tsx`) kept for file manager page

### Reused (no changes needed)
- `createShare`, `createShareAlias` — existing share API
- `notifyRecipients` — existing notification API
- `RecipientSelector` email validation pattern — regex reused in QuickShare recipient input

## Open Questions

_(none — all resolved during brainstorming)_

## Related

- B-21 — File deletion without share warning (tangentially related: shares reference files)
- 8.2 — Email Notifications (recipient notify uses existing MVP)
