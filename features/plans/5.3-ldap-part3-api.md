# 5.3 LDAP — Part 3: API Layer (Tasks 7–8)

Parent plan: `features/plans/5.3-ldap-implementation.md`

---

## Task 7: DTOs + Controller + Routes + Server Registration

**Files:**
- Create: `apps/server/src/modules/ldap/dto.ts`
- Create: `apps/server/src/modules/ldap/controller.ts`
- Create: `apps/server/src/modules/ldap/routes.ts`
- Modify: `apps/server/src/server.ts` (register routes + scheduler)

### Step 1: Create DTOs

Create `apps/server/src/modules/ldap/dto.ts`:

```typescript
import { z } from "zod";

export const LdapConfigSchema = z.object({
  enabled: z.boolean(),
  serverUrl: z.string().min(1, "Server URL is required"),
  bindDn: z.string().min(1, "Bind DN is required"),
  bindPassword: z.string(), // Empty string = keep existing
  searchBase: z.string().min(1, "Search base is required"),
  syncGroupDn: z.string().min(1, "Sync group DN is required"),
  usernameAttribute: z.string().min(1).default("sAMAccountName"),
  emailAttribute: z.string().min(1).default("mail"),
  displayNameAttribute: z.string().min(1).default("displayName"),
  syncIntervalMinutes: z.number().int().min(15).max(10080), // 15 min to 7 days
  useTls: z.boolean().default(true),
  appUrl: z
    .string()
    .url("Must be a valid URL")
    .nullable()
    .optional()
    .transform((v) => v || null),
});

export const LdapTestSchema = z.object({
  serverUrl: z.string().min(1),
  bindDn: z.string().min(1),
  bindPassword: z.string().min(1, "Bind password is required for testing"),
  searchBase: z.string().min(1),
  syncGroupDn: z.string().min(1),
  usernameAttribute: z.string().min(1).default("sAMAccountName"),
  emailAttribute: z.string().min(1).default("mail"),
  displayNameAttribute: z.string().min(1).default("displayName"),
  useTls: z.boolean().default(true),
});

export const SyncLogsQuerySchema = z.object({
  limit: z.coerce.number().int().min(1).max(100).default(20),
  offset: z.coerce.number().int().min(0).default(0),
});

export type LdapConfigInput = z.infer<typeof LdapConfigSchema>;
export type LdapTestInput = z.infer<typeof LdapTestSchema>;
export type SyncLogsQuery = z.infer<typeof SyncLogsQuerySchema>;
```

### Step 2: Create controller

Create `apps/server/src/modules/ldap/controller.ts`:

```typescript
import type { FastifyRequest, FastifyReply } from "fastify";
import { LdapConfigSchema, LdapTestSchema, SyncLogsQuerySchema } from "./dto.js";
import { LdapConfigRepository } from "./config.repository.js";
import { LdapSyncLogRepository } from "./sync.repository.js";
import { LdapSyncService } from "./sync.service.js";
import { LdapClient } from "./ldap.client.js";
import { encrypt } from "./encryption.js";
import { startScheduler, stopScheduler, getNextSyncAt } from "./sync.scheduler.js";
import { NotFoundError, ValidationError } from "../../utils/app-error.js";

const MASKED_PASSWORD = "••••••••";

export class LdapController {
  private configRepository = new LdapConfigRepository();
  private syncLogRepository = new LdapSyncLogRepository();
  private syncService = new LdapSyncService();

  async getConfig(_request: FastifyRequest, reply: FastifyReply) {
    const config = await this.configRepository.get();
    if (!config) {
      return reply.send({ configured: false });
    }
    return reply.send({
      configured: true,
      id: config.id,
      enabled: config.enabled,
      serverUrl: config.serverUrl,
      bindDn: config.bindDn,
      bindPassword: MASKED_PASSWORD,
      searchBase: config.searchBase,
      syncGroupDn: config.syncGroupDn,
      usernameAttribute: config.usernameAttribute,
      emailAttribute: config.emailAttribute,
      displayNameAttribute: config.displayNameAttribute,
      syncIntervalMinutes: config.syncIntervalMinutes,
      useTls: config.useTls,
      appUrl: config.appUrl,
      createdAt: config.createdAt,
      updatedAt: config.updatedAt,
    });
  }

  async updateConfig(request: FastifyRequest, reply: FastifyReply) {
    const data = LdapConfigSchema.parse(request.body);

    // Handle bind password
    let bindPassword: string;
    if (data.bindPassword && data.bindPassword !== MASKED_PASSWORD) {
      bindPassword = encrypt(data.bindPassword);
    } else {
      const existing = await this.configRepository.get();
      if (!existing) {
        throw new ValidationError(
          "Bind password is required for new configuration",
        );
      }
      bindPassword = existing.bindPassword;
    }

    const config = await this.configRepository.upsert({
      enabled: data.enabled,
      serverUrl: data.serverUrl,
      bindDn: data.bindDn,
      bindPassword,
      searchBase: data.searchBase,
      syncGroupDn: data.syncGroupDn,
      usernameAttribute: data.usernameAttribute,
      emailAttribute: data.emailAttribute,
      displayNameAttribute: data.displayNameAttribute,
      syncIntervalMinutes: data.syncIntervalMinutes,
      useTls: data.useTls,
      appUrl: data.appUrl ?? null,
    });

    // Reload scheduler
    if (config.enabled) {
      startScheduler(config.syncIntervalMinutes);
    } else {
      stopScheduler();
    }

    return reply.send({
      configured: true,
      id: config.id,
      enabled: config.enabled,
      serverUrl: config.serverUrl,
      bindDn: config.bindDn,
      bindPassword: MASKED_PASSWORD,
      searchBase: config.searchBase,
      syncGroupDn: config.syncGroupDn,
      usernameAttribute: config.usernameAttribute,
      emailAttribute: config.emailAttribute,
      displayNameAttribute: config.displayNameAttribute,
      syncIntervalMinutes: config.syncIntervalMinutes,
      useTls: config.useTls,
      appUrl: config.appUrl,
      createdAt: config.createdAt,
      updatedAt: config.updatedAt,
    });
  }

  async testConnection(request: FastifyRequest, reply: FastifyReply) {
    const data = LdapTestSchema.parse(request.body);
    const client = new LdapClient();
    const result = await client.testConnection({
      serverUrl: data.serverUrl,
      bindDn: data.bindDn,
      bindPassword: data.bindPassword,
      useTls: data.useTls,
      searchBase: data.searchBase,
      syncGroupDn: data.syncGroupDn,
      usernameAttribute: data.usernameAttribute,
      emailAttribute: data.emailAttribute,
      displayNameAttribute: data.displayNameAttribute,
    });
    return reply.send(result);
  }

  async triggerSync(_request: FastifyRequest, reply: FastifyReply) {
    const logId = await this.syncService.runSync("manual");
    return reply.send({ logId });
  }

  async getSyncLogs(request: FastifyRequest, reply: FastifyReply) {
    const { limit, offset } = SyncLogsQuerySchema.parse(request.query);
    const result = await this.syncLogRepository.list(limit, offset);
    return reply.send(result);
  }

  async getSyncLogDetail(request: FastifyRequest, reply: FastifyReply) {
    const { id } = request.params as { id: string };
    const log = await this.syncLogRepository.getById(id);
    if (!log) {
      throw new NotFoundError("Sync log not found");
    }
    return reply.send(log);
  }

  async getStatus(_request: FastifyRequest, reply: FastifyReply) {
    const config = await this.configRepository.get();
    const lastLog = await this.syncLogRepository.getLatest();

    return reply.send({
      configured: !!config,
      enabled: config?.enabled ?? false,
      syncInProgress: this.syncService.isSyncInProgress(),
      lastSync: lastLog
        ? {
            id: lastLog.id,
            status: lastLog.status,
            startedAt: lastLog.startedAt,
            completedAt: lastLog.completedAt,
            usersCreated: lastLog.usersCreated,
            usersUpdated: lastLog.usersUpdated,
            usersDeactivated: lastLog.usersDeactivated,
            usersReactivated: lastLog.usersReactivated,
            usersSkipped: lastLog.usersSkipped,
          }
        : null,
      nextSyncAt: getNextSyncAt(),
    });
  }
}
```

### Step 3: Create routes

Create `apps/server/src/modules/ldap/routes.ts`:

```typescript
import type { FastifyInstance } from "fastify";
import { z } from "zod";
import { LdapController } from "./controller.js";
import { createAdminPreValidation } from "../../middleware/admin-prevalidation.js";
import { ErrorResponseSchema } from "../../utils/error-response-schema.js";

export async function ldapRoutes(app: FastifyInstance) {
  const controller = new LdapController();
  const adminPreValidation = createAdminPreValidation({
    allowSetupBypass: false,
  });

  // GET /admin/ldap/config — Read LDAP configuration (bind password masked)
  app.get("/admin/ldap/config", {
    preValidation: [adminPreValidation],
    schema: {
      tags: ["ldap"],
      operationId: "getLdapConfig",
      summary: "Get LDAP configuration",
      response: {
        200: z.object({}).passthrough(),
        401: ErrorResponseSchema,
        403: ErrorResponseSchema,
      },
    },
    handler: controller.getConfig.bind(controller),
  });

  // PUT /admin/ldap/config — Create or update LDAP configuration
  app.put("/admin/ldap/config", {
    preValidation: [adminPreValidation],
    schema: {
      tags: ["ldap"],
      operationId: "updateLdapConfig",
      summary: "Create or update LDAP configuration",
      body: z.object({}).passthrough(),
      response: {
        200: z.object({}).passthrough(),
        400: ErrorResponseSchema,
        401: ErrorResponseSchema,
        403: ErrorResponseSchema,
      },
    },
    handler: controller.updateConfig.bind(controller),
  });

  // POST /admin/ldap/test — Test LDAP connection
  app.post("/admin/ldap/test", {
    preValidation: [adminPreValidation],
    schema: {
      tags: ["ldap"],
      operationId: "testLdapConnection",
      summary: "Test LDAP connection",
      body: z.object({}).passthrough(),
      response: {
        200: z.object({
          success: z.boolean(),
          memberCount: z.number(),
          message: z.string(),
        }),
        400: ErrorResponseSchema,
        401: ErrorResponseSchema,
        403: ErrorResponseSchema,
      },
    },
    handler: controller.testConnection.bind(controller),
  });

  // POST /admin/ldap/sync — Trigger manual sync
  app.post("/admin/ldap/sync", {
    preValidation: [adminPreValidation],
    schema: {
      tags: ["ldap"],
      operationId: "triggerLdapSync",
      summary: "Trigger manual LDAP sync",
      response: {
        200: z.object({ logId: z.string() }),
        401: ErrorResponseSchema,
        403: ErrorResponseSchema,
        409: ErrorResponseSchema,
      },
    },
    handler: controller.triggerSync.bind(controller),
  });

  // GET /admin/ldap/sync/logs — List sync history (paginated)
  app.get("/admin/ldap/sync/logs", {
    preValidation: [adminPreValidation],
    schema: {
      tags: ["ldap"],
      operationId: "getLdapSyncLogs",
      summary: "List LDAP sync history",
      querystring: z.object({
        limit: z.coerce.number().optional(),
        offset: z.coerce.number().optional(),
      }),
      response: {
        200: z.object({}).passthrough(),
        401: ErrorResponseSchema,
        403: ErrorResponseSchema,
      },
    },
    handler: controller.getSyncLogs.bind(controller),
  });

  // GET /admin/ldap/sync/logs/:id — Get sync log detail
  app.get("/admin/ldap/sync/logs/:id", {
    preValidation: [adminPreValidation],
    schema: {
      tags: ["ldap"],
      operationId: "getLdapSyncLogDetail",
      summary: "Get LDAP sync log detail",
      params: z.object({ id: z.string() }),
      response: {
        200: z.object({}).passthrough(),
        401: ErrorResponseSchema,
        403: ErrorResponseSchema,
        404: ErrorResponseSchema,
      },
    },
    handler: controller.getSyncLogDetail.bind(controller),
  });

  // GET /admin/ldap/status — Quick status overview
  app.get("/admin/ldap/status", {
    preValidation: [adminPreValidation],
    schema: {
      tags: ["ldap"],
      operationId: "getLdapStatus",
      summary: "Get LDAP sync status",
      response: {
        200: z.object({}).passthrough(),
        401: ErrorResponseSchema,
        403: ErrorResponseSchema,
      },
    },
    handler: controller.getStatus.bind(controller),
  });
}
```

### Step 4: Register routes and scheduler in server.ts

In `apps/server/src/server.ts`:

**Add imports** (after the existing route imports, around line 25):

```typescript
import { ldapRoutes } from "./modules/ldap/routes.js";
import { initSchedulerOnBoot, stopScheduler } from "./modules/ldap/sync.scheduler.js";
```

**Register routes** (after `app.register(groupRoutes)` at line 93):

```typescript
  app.register(ldapRoutes);
```

**Initialize scheduler** (after route registrations, before the cleanup intervals — around line 95):

```typescript
  // Initialize LDAP sync scheduler if configured
  await initSchedulerOnBoot();
```

**Stop scheduler on close** (in the `onClose` hooks area, around line 134):

```typescript
  stopScheduler();
```

### Step 5: Verify type-check

```bash
cd apps/server && pnpm run type-check
```

Expected: Clean compilation.

### Step 6: Commit

```bash
git add apps/server/src/modules/ldap/dto.ts apps/server/src/modules/ldap/controller.ts apps/server/src/modules/ldap/routes.ts apps/server/src/server.ts
git commit -m "feat(ldap): add API layer (DTOs, controller, routes) and register in server"
```

---

## Task 8: Integration Tests

**Files:**
- Create: `apps/server/src/__tests__/ldap.integration.test.ts`

### Step 1: Write integration tests

Create `apps/server/src/__tests__/ldap.integration.test.ts`:

```typescript
import { describe, it, expect, vi, beforeAll, afterAll, beforeEach } from "vitest";
import type { FastifyInstance } from "fastify";

// Mock Prisma BEFORE any app imports
vi.mock("../shared/prisma.js", () => ({
  prisma: {
    user: {
      count: vi.fn().mockResolvedValue(1),
      findUnique: vi.fn(),
      findMany: vi.fn().mockResolvedValue([]),
      create: vi.fn(),
      update: vi.fn(),
    },
    group: {
      findMany: vi.fn().mockResolvedValue([]),
    },
    ldapConfig: {
      findFirst: vi.fn().mockResolvedValue(null),
      create: vi.fn(),
      update: vi.fn(),
    },
    ldapSyncLog: {
      create: vi.fn().mockResolvedValue({
        id: "log-1",
        trigger: "manual",
        status: "running",
        startedAt: new Date(),
        completedAt: null,
        usersCreated: 0,
        usersUpdated: 0,
        usersDeactivated: 0,
        usersSkipped: 0,
        usersReactivated: 0,
        details: null,
        createdAt: new Date(),
      }),
      update: vi.fn(),
      findMany: vi.fn().mockResolvedValue([]),
      count: vi.fn().mockResolvedValue(0),
      findUnique: vi.fn(),
      findFirst: vi.fn().mockResolvedValue(null),
    },
    passwordReset: {
      create: vi.fn(),
    },
  },
}));

// Mock config service
vi.mock("../modules/config/service.js", () => ({
  getConfigValue: vi.fn().mockImplementation(async (key: string) => {
    const values: Record<string, string> = {
      maxLoginAttempts: "5",
      loginLockoutDuration: "900",
      smtpEnabled: "false",
      passwordResetTokenExpiration: "3600",
      appName: "OuiTransfer",
      smtpFromName: "OuiTransfer",
      smtpFromEmail: "noreply@test.com",
    };
    return values[key] ?? "";
  }),
  validatePasswordAuthDisable: vi.fn().mockResolvedValue(true),
  validateAllProvidersDisable: vi.fn().mockResolvedValue(true),
}));

// Mock token version validation
vi.mock("../modules/auth/token-version.js", () => ({
  validateTokenVersion: vi.fn().mockResolvedValue(true),
  invalidateTokenVersionCache: vi.fn(),
  incrementTokenVersion: vi.fn(),
}));

// Mock ldapts
vi.mock("ldapts", () => ({
  Client: vi.fn().mockImplementation(() => ({
    bind: vi.fn().mockResolvedValue(undefined),
    search: vi.fn().mockResolvedValue({ searchEntries: [] }),
    unbind: vi.fn().mockResolvedValue(undefined),
  })),
}));

// Mock the encryption module
vi.mock("../modules/ldap/encryption.js", () => ({
  encrypt: vi.fn().mockReturnValue("encrypted-value"),
  decrypt: vi.fn().mockReturnValue("decrypted-password"),
}));

// Mock the scheduler
vi.mock("../modules/ldap/sync.scheduler.js", () => ({
  startScheduler: vi.fn(),
  stopScheduler: vi.fn(),
  getNextSyncAt: vi.fn().mockReturnValue(null),
  isSchedulerRunning: vi.fn().mockReturnValue(false),
  initSchedulerOnBoot: vi.fn().mockResolvedValue(undefined),
  syncService: {
    runSync: vi.fn().mockResolvedValue("log-1"),
    isSyncInProgress: vi.fn().mockReturnValue(false),
  },
}));

import { prisma } from "../shared/prisma.js";

const mockedPrisma = vi.mocked(prisma);

let app: FastifyInstance;

function signTestToken(userId: string, isAdmin = false): string {
  const jwt = app.jwt.sign({ userId, isAdmin, tokenVersion: 0 });
  return app.signCookie(jwt);
}

async function getCsrf(): Promise<{ csrfToken: string; csrfCookie: string }> {
  const csrfRes = await app.inject({
    method: "GET",
    url: "/csrf-token",
  });
  const { token: csrfToken } = csrfRes.json();
  const csrfCookie = csrfRes.cookies.find((c) => c.name === "_csrf");
  return { csrfToken, csrfCookie: csrfCookie!.value };
}

function adminHeaders(csrfToken: string, csrfCookie: string, userId = "admin-1") {
  const token = signTestToken(userId, true);
  return {
    "content-type": "application/json",
    cookie: `token=${token}; _csrf=${csrfCookie}`,
    "x-csrf-token": csrfToken,
  };
}

function adminReadHeaders(userId = "admin-1") {
  const token = signTestToken(userId, true);
  return {
    cookie: `token=${token}`,
  };
}

function nonAdminHeaders() {
  const token = signTestToken("user-1", false);
  return {
    cookie: `token=${token}`,
  };
}

beforeAll(async () => {
  vi.stubEnv("JWT_SECRET", "a]test-jwt-secret-32-chars-long!");
  vi.stubEnv("CSRF_SECRET", "b]test-csrf-secret-32chars-long!");
  vi.stubEnv("COOKIE_SECRET", "c]test-cookie-secret-32chars-lon");
  vi.stubEnv("ENCRYPTION_SECRET", "d]test-encrypt-secret-32chars!!");
  vi.stubEnv("NODE_ENV", "test");

  const { buildApp } = await import("../app.js");
  app = await buildApp();

  const { ldapRoutes } = await import("../modules/ldap/routes.js");
  app.register(ldapRoutes);
  await app.ready();
});

afterAll(async () => {
  await app.close();
  vi.unstubAllEnvs();
});

beforeEach(() => {
  vi.clearAllMocks();
  // Reset default mock values
  mockedPrisma.user.count.mockResolvedValue(1);
  mockedPrisma.ldapConfig.findFirst.mockResolvedValue(null);
  mockedPrisma.ldapSyncLog.findMany.mockResolvedValue([]);
  mockedPrisma.ldapSyncLog.count.mockResolvedValue(0);
  mockedPrisma.ldapSyncLog.findFirst.mockResolvedValue(null);
});

describe("LDAP Integration Tests", () => {
  describe("GET /admin/ldap/config", () => {
    it("should return 401 for unauthenticated requests", async () => {
      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/config",
      });
      expect(res.statusCode).toBe(401);
    });

    it("should return 403 for non-admin users", async () => {
      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/config",
        headers: nonAdminHeaders(),
      });
      expect(res.statusCode).toBe(403);
    });

    it("should return configured: false when no config exists", async () => {
      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/config",
        headers: adminReadHeaders(),
      });
      expect(res.statusCode).toBe(200);
      expect(res.json()).toEqual({ configured: false });
    });

    it("should return config with masked password when configured", async () => {
      mockedPrisma.ldapConfig.findFirst.mockResolvedValue({
        id: "config-1",
        enabled: true,
        serverUrl: "ldaps://ad.corp.local:636",
        bindDn: "cn=svc,dc=corp,dc=local",
        bindPassword: "encrypted-value",
        searchBase: "DC=corp,DC=local",
        syncGroupDn: "CN=Sync,DC=corp,DC=local",
        usernameAttribute: "sAMAccountName",
        emailAttribute: "mail",
        displayNameAttribute: "displayName",
        syncIntervalMinutes: 360,
        useTls: true,
        appUrl: "https://transfer.corp.local",
        createdAt: new Date(),
        updatedAt: new Date(),
      });

      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/config",
        headers: adminReadHeaders(),
      });
      expect(res.statusCode).toBe(200);
      const body = res.json();
      expect(body.configured).toBe(true);
      expect(body.bindPassword).toBe("••••••••");
      expect(body.serverUrl).toBe("ldaps://ad.corp.local:636");
    });
  });

  describe("PUT /admin/ldap/config", () => {
    it("should require admin auth", async () => {
      const { csrfToken, csrfCookie } = await getCsrf();
      const res = await app.inject({
        method: "PUT",
        url: "/admin/ldap/config",
        headers: {
          "content-type": "application/json",
          cookie: `token=${signTestToken("user-1", false)}; _csrf=${csrfCookie}`,
          "x-csrf-token": csrfToken,
        },
        payload: JSON.stringify({ serverUrl: "ldaps://test" }),
      });
      expect(res.statusCode).toBe(403);
    });

    it("should create new config with encrypted password", async () => {
      mockedPrisma.ldapConfig.findFirst.mockResolvedValue(null);
      mockedPrisma.ldapConfig.create.mockResolvedValue({
        id: "config-1",
        enabled: false,
        serverUrl: "ldaps://ad.corp.local:636",
        bindDn: "cn=svc,dc=corp,dc=local",
        bindPassword: "encrypted-value",
        searchBase: "DC=corp,DC=local",
        syncGroupDn: "CN=Sync,DC=corp,DC=local",
        usernameAttribute: "sAMAccountName",
        emailAttribute: "mail",
        displayNameAttribute: "displayName",
        syncIntervalMinutes: 360,
        useTls: true,
        appUrl: null,
        createdAt: new Date(),
        updatedAt: new Date(),
      });

      const { csrfToken, csrfCookie } = await getCsrf();
      const res = await app.inject({
        method: "PUT",
        url: "/admin/ldap/config",
        headers: adminHeaders(csrfToken, csrfCookie),
        payload: JSON.stringify({
          enabled: false,
          serverUrl: "ldaps://ad.corp.local:636",
          bindDn: "cn=svc,dc=corp,dc=local",
          bindPassword: "my-secret-password",
          searchBase: "DC=corp,DC=local",
          syncGroupDn: "CN=Sync,DC=corp,DC=local",
          usernameAttribute: "sAMAccountName",
          emailAttribute: "mail",
          displayNameAttribute: "displayName",
          syncIntervalMinutes: 360,
          useTls: true,
        }),
      });

      expect(res.statusCode).toBe(200);
      const body = res.json();
      expect(body.bindPassword).toBe("••••••••");
    });
  });

  describe("POST /admin/ldap/test", () => {
    it("should test connection and return result", async () => {
      const { csrfToken, csrfCookie } = await getCsrf();
      const res = await app.inject({
        method: "POST",
        url: "/admin/ldap/test",
        headers: adminHeaders(csrfToken, csrfCookie),
        payload: JSON.stringify({
          serverUrl: "ldaps://ad.corp.local:636",
          bindDn: "cn=svc,dc=corp,dc=local",
          bindPassword: "test-password",
          searchBase: "DC=corp,DC=local",
          syncGroupDn: "CN=Sync,DC=corp,DC=local",
          usernameAttribute: "sAMAccountName",
          emailAttribute: "mail",
          displayNameAttribute: "displayName",
          useTls: true,
        }),
      });
      expect(res.statusCode).toBe(200);
      const body = res.json();
      expect(body).toHaveProperty("success");
      expect(body).toHaveProperty("memberCount");
      expect(body).toHaveProperty("message");
    });
  });

  describe("POST /admin/ldap/sync", () => {
    it("should trigger manual sync", async () => {
      const { csrfToken, csrfCookie } = await getCsrf();
      const res = await app.inject({
        method: "POST",
        url: "/admin/ldap/sync",
        headers: adminHeaders(csrfToken, csrfCookie),
      });
      expect(res.statusCode).toBe(200);
      expect(res.json()).toHaveProperty("logId");
    });
  });

  describe("GET /admin/ldap/sync/logs", () => {
    it("should return paginated logs", async () => {
      mockedPrisma.ldapSyncLog.findMany.mockResolvedValue([]);
      mockedPrisma.ldapSyncLog.count.mockResolvedValue(0);

      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/sync/logs?limit=10&offset=0",
        headers: adminReadHeaders(),
      });
      expect(res.statusCode).toBe(200);
      const body = res.json();
      expect(body).toHaveProperty("logs");
      expect(body).toHaveProperty("total");
    });
  });

  describe("GET /admin/ldap/sync/logs/:id", () => {
    it("should return 404 for non-existent log", async () => {
      mockedPrisma.ldapSyncLog.findUnique.mockResolvedValue(null);

      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/sync/logs/non-existent",
        headers: adminReadHeaders(),
      });
      expect(res.statusCode).toBe(404);
    });

    it("should return log detail when found", async () => {
      mockedPrisma.ldapSyncLog.findUnique.mockResolvedValue({
        id: "log-1",
        startedAt: new Date(),
        completedAt: new Date(),
        status: "success",
        trigger: "manual",
        usersCreated: 2,
        usersUpdated: 1,
        usersDeactivated: 0,
        usersSkipped: 0,
        usersReactivated: 0,
        details: "[]",
        createdAt: new Date(),
      });

      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/sync/logs/log-1",
        headers: adminReadHeaders(),
      });
      expect(res.statusCode).toBe(200);
      expect(res.json().id).toBe("log-1");
    });
  });

  describe("GET /admin/ldap/status", () => {
    it("should return status overview", async () => {
      const res = await app.inject({
        method: "GET",
        url: "/admin/ldap/status",
        headers: adminReadHeaders(),
      });
      expect(res.statusCode).toBe(200);
      const body = res.json();
      expect(body).toHaveProperty("configured");
      expect(body).toHaveProperty("enabled");
      expect(body).toHaveProperty("syncInProgress");
      expect(body).toHaveProperty("lastSync");
      expect(body).toHaveProperty("nextSyncAt");
    });
  });
});
```

### Step 2: Run integration tests

```bash
cd apps/server && pnpm vitest run src/__tests__/ldap.integration.test.ts
```

Expected: All tests PASS. Fix any mock wiring issues.

### Step 3: Run full server test suite

```bash
cd apps/server && pnpm test
```

Expected: All existing tests still pass, no regressions.

### Step 4: Commit

```bash
git add apps/server/src/__tests__/ldap.integration.test.ts
git commit -m "test(ldap): add integration tests for all 7 LDAP endpoints"
```
