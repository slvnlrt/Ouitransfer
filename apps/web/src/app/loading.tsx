// This Suspense fallback may render before NextIntlClientProvider is mounted,
// so it must be a plain server component with no i18n hooks.
export { PageLoading as default } from "@/components/layout/page-loading";
